// Destructured bytes
//
// When using this all sizes will be measured in bits instead of bytes
//
// size: byte length of this bit struct
// elements: array of sub elements
//
// returns: parser function that returns array of decoded items in same order as elements array
function BitStruct(name, {size = 1, elements}) {

}

module.exports = BitStruct;
